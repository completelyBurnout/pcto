#include <stdio.h>

int main() // modulo si scrive con % e risultato è il resto
{
    int a=1;
    scanf("%d", a);

    while(a<=100) 
        {
            int c=0
            if(n%a==0)
            {
               c=c+1 //c è il numero di divisori
            }
        }
}